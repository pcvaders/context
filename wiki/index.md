# AI Agentic Wiki — Index

> Master index of all wiki pages. GENERATED by generate_wiki_index.py — do not edit by hand.

## Structure

- **[[concepts/]]** — Fundamental ideas: RAG, MCP, context windows, agent memory, semantic search, embeddings, etc.
- **[[entities/]]** — Tools, models, people, orgs: Claude Code, Karpathy, Smart Connections, Obsidian, etc.
- **[[sources/]]** — Summaries of ingested sources: papers, gists, articles, repos
- **[[syntheses/]]** — Cross-cutting analysis: comparisons, frameworks, emergent patterns
- **[[pinokio/]]** — Pinokio launcher + Windows 11 local AI stack
- **[[incidents/]]** — Resolved incident writeups: symptom, root cause, fix, lesson

## Concepts

*19 pages*

| Page | Summary |
|---|---|
| [[agent-native-cli]] | Design pattern for CLI tools optimised for LLM agent consumption — auto-JSON output, typed exit codes, --compact mode, stdin piping, and local SQLite persistence. |
| [[ai-analysis-skill]] | 38-framework diagnostic tool for AI systemic analysis. First live use 2026-05-25 confirmed structural failure pattern in Claude. |
| [[ai-cost-observability]] | Tools and practices for measuring, attributing, and optimizing token spend across AI coding agents. |
| [[claude-ai-context-loading]] | Pattern for loading persistent context at session start across Claude surfaces — Project Instructions, CLAUDE.md, hooks. |
| [[claude-code-skills]] | The plugin/skill system for Claude Code — markdown files that load additional behaviors, workflows, and slash commands into the agent. |
| [[comfyui]] | summary: Node-based Stable Diffusion / image-gen workflow tool referenced across Higgsfield and Win11-CL pipeline pages. |
| [[higgsfield]] | summary: AI image/video generation platform (Soul, Seedance, Marketing Studio) used across the Architect pipeline. |
| [[higgsfield-comfyui-pipeline-architecture]] | Unified AI film & content production pipeline. Higgsfield handles hosted fast iteration and branded ad output. ComfyUI (Win11/RTX 5070ti) handles local precision, LoRA-driven camera control, and zero-cost bulk generation. Reconstructed 2026-05-31 from UI mapping session. |
| [[ingest]] | Adding a new source to the wiki and integrating its knowledge into existing pages. |
| [[lint]] | Periodic health check of the wiki — fixing contradictions, orphan pages, and stale claims. |
| [[llm-wiki]] | A pattern for building personal knowledge bases where the LLM incrementally builds and maintains a persistent wiki rather than re-deriving knowledge from raw documents on every query. |
| [[ltx-video]] | summary: Video generation model referenced in the Higgsfield/ComfyUI pipeline architecture notes. |
| [[mcp]] | Open standard by Anthropic enabling LLM agents to connect to external tools, data sources, and services via a unified server/client protocol. |
| [[query]] | Asking questions against the wiki and filing good answers back as new pages. |
| [[rag]] | Traditional retrieval-augmented generation — the pattern the LLM Wiki replaces. |
| [[seedance]] | summary: Video generation model (Higgsfield) used for cinematic/narrative shot prompts. |
| [[skill-ecosystem]] | 4-marketplace Claude Code skill stack: superpowers, antigravity, karpathy, caveman. How skills load, invoke, and interact. |
| [[soul-id]] | summary: Higgsfield identity-training feature for face-consistent character generation. |
| [[token-compression]] | Techniques that reduce LLM output token count while preserving technical accuracy — cutting cost, latency, and context pressure. |

## Entities

*31 pages*

| Page | Summary |
|---|---|
| [[agentic-os]] | Unified homelab command center and agent operating dashboard serving telemetry, 3D connectome, multi-theme runtime, and living animations. |
| [[amuseai-vertex-integration]] | TensorStack AmuseAI has no native pipeline for Gemini/Imagen/Veo — custom Python files reroute those model slots through Vertex AI (gcloud ADC), working as of 2026-06-11. |
| [[bumble-credential-hunt-incident]] | A Buzz agent on a weak free model self-escalated into a home-wide search for its own signing key. Worth reading before wiring any agent with shell access. |
| [[buzz-agent-harness-config]] | How to wire Buzz agents to external ACP backends, and the traps. Verified 2026-07-29 on Buzz v0.5.0. Full identity rotation done 2026-07-29 — all agents rebuilt post-wipe. |
| [[claude-code]] | Anthropic's agentic coding tool — one of the recommended LLM writers for this wiki pattern. |
| [[claude-code-skill-enforcer-windows-bug]] | skill-enforcer.py's hook uses `os.getppid()` as a session key, which is unstable on Windows — every hook invocation gets a fresh subprocess with a different parent PID, so it never matches its own flag and permanently blocks Edit/Write/Bash after any skill invocation. |
| [[comfyui-pinokio-blackwell]] | Two ComfyUI installs live side by side under Pinokio on Win11: the original (untouched) and an updated clone built for RTX 50-series (Blackwell) with SageAttention. |
| [[cutmaster-ai-davinci-tools]] | `davinci-tools.html` dashboard wired to a 284-tool `cutmaster_ai` backend; verified end-to-end via a real DaVinci Resolve timeline cut on 2026-06-13. Known gap: ffmpeg not on PATH. |
| [[cutmaster-ai-git-hosting]] | cutmaster-ai's GitHub account was suspended; hosting moved to Codeberg (Forgejo) under a new username. Push auth from this Win11 machine is still unresolved. |
| [[deepseek-harness]] | Local AI inference and orchestration harness running on port 3080 routing DeepSeek reasoning and coding models. |
| [[forgejo-mirror]] | Self-hosted Forgejo on Proxmox LXC 106 that mirrors both GitHub accounts, cross-mirrors them, and pushes offsite to Google Drive — four independent copies of every repo. LAN-only. (as of 2026-06) |
| [[homelab]] | summary: voyager1's multi-machine Proxmox/Mac/Win11 fleet — the umbrella entity referenced across infra pages. |
| [[interlinked]] | Claude Code skill that activates the AI LLM Wiki assistant persona — handles INGEST, QUERY, and LINT operations on the vault. |
| [[isle-of-dogs-project]] | summary: Stop-motion-style AI film project (puppet conversion, character work) referenced from the Higgsfield pipeline notes. |
| [[julius-brussee]] | Creator of the caveman token compression skill and the broader caveman ecosystem for LLM agents. |
| [[karpathy-andrej]] | Co-founder of OpenAI, former Tesla AI Director, creator of the LLM Wiki pattern. |
| [[litellm-proxy]] | Local OpenAI-compatible proxy on port 4000 routing calls to Ollama and cloud LLMs. Auto-starts via LaunchAgent. |
| [[notebooklm-mcp]] | NotebookLM MCP setup for Claude Code and Cowork (Claude Desktop) — auth via saved Google session. |
| [[obsidian]] | Local-first markdown note-taking app — the reading interface for this LLM Wiki. |
| [[obsidian-agent-bridge]] | summary: The connective layer between the Obsidian vault and AI agents — recall CLI (3 corpora), clip tools, skills, and hooks, with honest-absence guarantees. |
| [[obsidian-wiki-memory]] | Claude Code skill that loads the AI LLM Wiki vault context into any session at startup via smart-clip search. |
| [[pcg1-directors-prep]] | summary: Prep/reference materials entity referenced from the ComfyUI master synthesis page. |
| [[pinecone]] | Managed vector database service — primary alternative to local embeddings for RAG pipelines at scale. |
| [[proxmox]] | summary: Virtualization host running the homelab's LXC containers (Forgejo, n8n, recall-server, etc.). |
| [[reachy-mini]] | Robotics platform and autonomous AI Radio DJ integrating Spotify Connect, multi-sink audio routing, and expressive head kinematics. |
| [[renderzero-vertex-patch]] | Placeholders: replace `YOUR_PROJECT` with your GCP project id and `YOUR_ACCOUNT` with your Google account. Do not commit real values. |
| [[semantic-clip]] | Local CLI tool that replaces smart-connections-mcp — reads pre-computed `.smart-env/` embeddings directly for zero-network, zero-MCP semantic vault search. |
| [[smart-clip]] | Three-tier local search orchestrator: keyword → semantic → NotebookLM fallback over the AI LLM Wiki vault. |
| [[smart-connections]] | Obsidian plugin providing AI-powered semantic search and note linking via local embeddings; exposed to Claude Code via the smart-connections-mcp server. |
| [[smart-connections-mcp]] | ~~MCP server that bridges Obsidian Smart Connections embeddings to Claude Code.~~ **DEPRECATED (2026-05-22) — replaced by  CLI. MCP disabled in `~/.claude/settings.json`. Do not use MCP tools.** |
| [[wiki-clip]] | CLI keyword search tool over AI LLM Wiki + Claude Skills frontmatter (Tier 1 of smart-clip pipeline). |

## Sources

*65 pages*

| Page | Summary |
|---|---|
| [[ai-infrastructure-stack-mar2026]] | Mar 2026 conversation covering Anthropic inference cost management, the Aug-Sep 2025 quality degradation incident, OpenClaw agent economics, TurboQuant KV compression, ARM's first datacenter CPU. |
| [[antigravity-awesome-skills]] | Community library of 1,431 reusable SKILL.md playbooks for Claude Code, installable as a Claude Code plugin marketplace with 37 curated bundles. |
| [[caveman-julius-brussee]] | Claude Code skill that compresses LLM output ~65% by enforcing caveman-style brevity without sacrificing technical accuracy. |
| [[claude-chat-accessing-notebooklm]] | Chat opened with: can you see my notebooklm now? |
| [[claude-chat-ai-memory-notebooklm-update]] | Chat opened with: so i just found this in our past chats today, can you give me an update for it as there is nothing in the Notebooklm? Perfect! Let me create an 'AI Memory NotebookLM' for tracking our conversations… |
| [[claude-chat-aqara-motion-sensor-setup]] | **Conversation Overview** This conversation focused on Home Assistant automation management and dashboard setup. The person is managing a smart home system with multiple PIR sensors across ground floor zones, and added a new Aqara High Human Sensor to the ground floor. A significant portion of the conversation involved clarifying the sensor's actual capabilities — the person discovered through experience that the Aqara is PIR-based rather than true mmWave, and that even their existing "24G Human PIR 1" device only activates its radar component after an initial PIR trigger due to power constraints of battery-operated mmWave devices. The person decided to keep the Aqara installed next to `binary_sensor.human_pir_gnd` in the centre of the main and kitchen rooms to empirically test whether its occupancy entity holds when someone is stationary or asleep. The main automation worked on was `automation.living_room_lamp_pir_presence_control` (ID: `1774136816638`). Several iterations were required due to bugs introduced during edits: a `switch.basement_lamp_1 = off` condition was incorrectly blocking the off sequence when the TV was on, and a `time after 12:15` condition was incorrectly added to the basement TV off action. The person explicitly noted Claude was "being creative" when it should have kept changes simple and explicit. The final correct automation state includes all four sensors (`binary_sensor.human_pir_gnd`, `binary_sensor.mums_pir_2`, `binary_sensor.pir_kitchen_gnd_floor`, `binary_sensor.aqara_high_human_sensor_occupancy`) in both motion ON triggers and no-presence OFF triggers and the all-clear condition. The lamp (`switch.double_gang_1_mums_switch_2`) turns on below 2550lx illuminance; the Basement TV (`switch.basement_lamp_1`) turns on after 12:15 on motion, and unconditionally at 10:45am on Sundays. Both turn off unconditionally with no gating when all four sensors clear. A secondary thread involved installing UI Lovelace Minimalist v1.5.2 from HACS. The config errors on the Example View dashboard were traced to missing button-card dependency, which was installed and registered. However the person then shared a screenshot of their actual goal — a richly designed dashboard with weather forecast, clock, temperature graphs, camera panels, and device controls — which appears to require Mushroom Cards, ApexCharts Card, and a weather card rather than UI Lovelace Minimalist. That work was in progress when the conversation shifted back to the automation. The conversation ended with the person reporting the Aqara occupancy sensor is sluggish (taking over 5 minutes to clear) and had a phantom occupied state overnight, with the MCP server becoming unresponsive before diagnosis could be completed. **Tool Knowledge** Home Assistant automation retrieval uses `ha_config_get_automation` with the `identifier` parameter set to the full entity ID (e.g. `automation.living_room_lamp_pir_presence_control`). When setting automations with `ha_config_set_automation`, both `identifier` and `config` parameters are required; the config must include the `id` field matching the original automation ID (`1774136816638`) to avoid creating duplicates. Dashboard resources are managed via `ha_config_list_dashboard_resources` and `ha_config_set_dashboard_resource` — UI Lovelace Minimalist v1.5.x auto-registers button-card with a `?hacstag=` query string, so manually adding a duplicate button-card resource should be avoided and cleaned up with `ha_config_delete_dashboard_resource`. HACS Lovelace installs do not automatically register JS resources; `ha_config_set_dashboard_resource` with `resource_type: module` is required separately. When the MCP server becomes unresponsive mid-conversation, the person resolves this by restarting the Home Assistant MCP Server add-on from Settings → Add-ons. |
| [[claude-chat-aqara-sensor-occupancy-delay-issue]] | **Conversation Overview** This session focused on Home Assistant diagnostics, automation repair, and system workflow improvements, with the person using the Claude Mac app integrated directly with Home Assistant via MCP tools. The person manages a multi-floor property with a Zigbee-based smart home system running ZHA on a Sonoff USB Dongle E, with a Sonoff USB Dongle P ordered to enable Zigbee2MQTT alongside ZHA permanently. The session began with diagnostics on a new Aqara High Human Sensor (MMWave radar, ground floor) that was taking 15 minutes to clear occupancy. Claude identified the root cause as the combination of a 60-second Detection Interval and 120-second Occupied-to-Unoccupied Delay, and attempted to write corrected values (15s interval, 30s delay) via ZHA MCP calls. The writes silently failed and reverted on the next device heartbeat, confirming a known ZHA limitation for this sensor type. The fix is deferred until the Sonoff USB Dongle P arrives and Zigbee2MQTT is set up. The Z2M migration plan was also clarified: ZHA and Z2M will run permanently side by side on separate dongles, with no decommissioning of ZHA required. A second issue involved the 1st Floor Corridor Lights automation getting stuck on after a PIR sensor entered an Unknown state. The person had already replaced the faulty sensor (binary_sensor.pir_stairs_corridor) with a new one (binary_sensor.pir_top_of_stairs_1st_floor), but editing the automation via the HA UI had only updated the trigger step while leaving stale entity references in the wait_for_trigger and condition steps. Claude corrected the full automation config via MCP, aligning all three steps to the new entity ID and confirming the 1-minute timeout the person had intentionally set. A major skill file update was produced incorporating: a critical warning that "Human Sensor" naming does not reliably indicate MMWave sensor type, an MQTT-Only Settings section documenting parameters that cannot be written via ZHA, the Z2M migration plan, and the corrected automation. The person also established "Interlinked" as a master keyword that triggers automatic production of both a session summary and updated skill file as downloadable outputs at the end of every session, without needing to be asked. This keyword convention was embedded into the skill file itself so future sessions inherit the behaviour. **Tool Knowledge** Claude used the Home Assistant MCP integration throughout this session. The ha_get_states tool worked reliably when passed a list of entity IDs simultaneously, making it efficient to pull sensor health, config parameters, and device metadata in a single call. The ha_get_history tool used a start_time value expressed as a relative duration string (e.g., "3h") with significant_changes_only set to True and a limit of 100, which returned a clean state-change log suitable for diagnosing occupancy hold behaviour. The ha_call_service tool for the number domain using set_value dispatched without error but the Aqara sensor silently rejected the writes and reverted values on the next device report cycle — this is not a tool failure but a ZHA-to-device write-back limitation specific to this sensor model; future sessions should not attempt these writes via ZHA and should go directly to the Z2M config panel once the USB Dongle P is installed. The ha_config_set_automation tool using the automation's numeric id field (not the entity_id slug) successfully overwrote the full automation config in one operation, which is more reliable than HA UI editing because it replaces all steps atomically rather than patching only touched fields — this is the preferred method for any automation update to avoid stale entity reference bugs. |
| [[claude-chat-canon-r5c-self-timer-photo-mode]] | **Conversation Overview** The person asked for help navigating the Canon EOS R5C camera's self-timer/drive mode settings in photo mode. Claude provided step-by-step instructions covering two methods: using the Q (Quick Control) button to access Drive mode via the Quick menu, and navigating through the main Shooting menu. Claude noted that the R5C's physical photo/video switch must be set to photo mode for these options to appear, and explained the practical difference between the 2-second and 10-second timer options (the 2-second delay being useful for reducing vibration when shooting on a tripod). The person then asked to see an image of the Q button's location. Claude attempted multiple image searches and web searches to locate Canon's official labeled rear-panel diagram, using tools including image search and web fetch. The official Canon manual page (cam.start.canon) was successfully retrieved, confirming the Q button's official designation as the "Quick Control button" (item 12 in Canon's parts list). Because image searches returned standard R5 review photos rather than the official labeled line-drawing, Claude generated a custom SVG schematic diagram of the R5C rear panel with the Q button highlighted in amber, showing its position to the right of the LCD screen, below the joystick, and above the INFO button. Claude also provided direct links to Canon's official online manual parts page and the Canon U.S.A. R5C support page for the official PDF manual. **Tool Knowledge** Image search for official camera manual diagrams consistently returned third-party review photos rather than manufacturer line-drawings; fetching the official manual URL directly (cam.start.canon HTML manual pages) proved more reliable for confirming exact button names, positions, and item numbers from Canon's documentation. When the official diagram could not be surfaced as an image, generating a custom SVG schematic based on confirmed manual data was the effective fallback approach. |
| [[claude-chat-canva-design-examples]] | Chat opened with: Hey Claude can you give me an example of your talents using Canva |
| [[claude-chat-casual-greeting]] | Chat opened with: hey Claude hows things with you today? |
| [[claude-chat-claude-design-dropdown-menu-location]] | Chat opened with: can you tell me where the dropdown menu is for claude design? |
| [[claude-chat-clean-install-of-immich-on-proxmox]] | Chat opened with: So can you help perform a clean install of Immich on my Lenovo Proxmox? |
| [[claude-chat-cowork-task-setup-with-missing-folders]] | Chat opened with: i am trying to setup a task in cowork, to use this project and search for clippings but i cannot see any folders etc here? |
| [[claude-chat-creating-a-content-bible-for-ai-film]] | **Conversation Overview** This conversation covered a wide range of AI filmmaking and tooling topics, along with ongoing work on the person's self-built LLM wiki system. The person is actively working on AI film production and has deep familiarity with tools like Seedance, LTX Video, and ComfyUI. The discussion compared Seedance 2.0 and LTX 2.3's camera control capabilities in detail, covering rack/pull focus, dolly zoom, Hitchcock zoom, and other cinematic moves. Key findings were that Seedance handles prompt-driven cinematography language more reliably while LTX 2.3 offers higher resolution but requires LoRAs (specifically the Cameraman IC-LoRA) for precise camera control. The person also learned that LTX 2.3 is developed and owned by Lightricks, an Israeli AI company, and that the "Pro" designation refers to a checkpoint variant rather than a separate model. The latter portion of the conversation focused on the person's existing AI Agentic Wiki, which they have already built and stored in a GitHub repository at `PCGamesplay1/Claude-skills`. The vault structure includes `wiki/`, `raw/`, and `claude-sessions/` directories with INGEST, QUERY, and LINT operations. The person's primary concern was a sync issue — the last confirmed GitHub push was at 2:00 AM that morning (May 27, 2026), but the wiki log only showed entries up to May 22, suggesting a five-day gap. The session ended with Claude attempting to diagnose the `sync.sh` file but being unable to locate it in the repo, and then correctly being reminded by the person that they are on mobile and cannot easily run terminal commands or paste output. The person demonstrated a preference for direct, practical diagnosis and was comfortable correcting Claude's assumptions, including pointing out the mobile context as a constraint. The GitHub raw URL bridge (`raw.githubusercontent.com`) is the established method for Claude to read wiki content within sessions. **Tool Knowledge** Web fetch against `https://raw.githubusercontent.com/PCGamesplay1/Claude-skills/main/wiki/index.md` and `wiki/log.md` successfully retrieves current wiki state. These are the canonical session-start URLs to load for context. Fetching `wiki/sync.sh` and `sync.sh` at repo root both returned no content, meaning the sync script is not committed to the repo and must be accessed locally on the person's Mac. When diagnosing sync issues on mobile, the most accessible diagnostic is the GitHub commit history for `wiki/index.md` viewed in browser, as commit timestamps reveal the true last successful push independent of any local log entries. |
| [[claude-chat-creating-a-siri-shortcut-to-open-claude]] | Chat opened with: Hey, Claude. Is there a way of linking up a Siri or developing a new shortcut to just open your chat system? |
| [[claude-chat-dog-to-isle-of-dogs-puppet-conversion-prompt]] | Chat opened with: /banana-pro-director create a prompt for AI image creation using GPT 2 and Nano Banana 2 and Pro to convert this dog into an Isle of Dogs Puppet with 4 different scales |
| [[claude-chat-enhancing-ai-analysis-skill-capabilities]] | **Conversation Overview** The person worked with Claude on a multi-stage project to update, correct, and expand their custom "ai-analysis" skill (SKILL.md), a sophisticated multi-framework analytical system for examining AI developments through both systems analysis and wisdom tradition lenses. The session began with Claude building an interactive AI Analysis Engine widget that demonstrated the skill's capabilities via the Anthropic API with streaming output, framework selection pills, and example prompts. Claude then identified three inconsistencies in the skill file: the version field in the front matter was frozen at 1.2 while the changelog showed v1.4, the origin block omitted version histories for 1.1, 1.3, and 1.4, and the description field didn't reflect the full scope of frameworks present in the document body. The session evolved into a substantive expansion of the skill itself. The person directed Claude to add Sacred Geometry (Framework 29) and The Cosmos as Ground (Framework 30), recognizing that geometric and astronomical traditions were implicitly present throughout the skill but never named directly. Claude wrote both frameworks in full, matching the skill's established voice and structure. The person then identified that a macrocosm-to-microcosm principle was the underlying connective logic of all 30 frameworks, and Claude added this as an explicit meta-principle section ("As Above, So Below") positioned between the Prime Directive and the framework list. A two-line addition to the Founding Insight closing was also made to reflect the new cosmic scope. The skill was versioned to 1.5 throughout. A recurring practical constraint was that Claude cannot write directly to the skills directory or trigger a server-side refresh — all changes required the person to manually download the output file and re-upload it to their skill system. A numbering error (two frameworks labeled 30, Framework 28 missing) was caught and corrected before final output. The description field character count was carefully managed to stay under the 1024-character platform limit, landing at 941 characters. The person confirmed the skill needed to be deleted and re-uploaded to force a fresh index, with the real test being whether a new conversation draws on the updated frameworks. **Tool Knowledge** Claude used `view`, `str_replace`, `bash_tool`, and `present_files` tools throughout. The skill file lived at `/mnt/skills/user/ai-analysis/SKILL.md` (read-only) and required copying to `/home/claude/SKILL.md` for editing before copying outputs to `/mnt/user-data/outputs/SKILL.md` for download. The person's uploaded file appeared at `/mnt/user-data/uploads/Nature_Cycle_14Mar26_SKILL_5_.md` and needed to be copied to the working directory to serve as the editing base after manual uploads. The YAML `description` field uses a folded scalar (`>`), so character count verification required a Python script that strips leading whitespace from each line and joins with spaces before counting — the raw line count in the file does not reflect the actual character count the platform validates against. Framework numbering errors are best caught with `grep -n "^### [0-9]*\." /home/claude/SKILL.md` to list all numbered headings with line numbers before finalizing output. |
| [[claude-chat-environment-check-skill-status]] | Chat opened with: okay is the env check skill working now |
| [[claude-chat-finding-previous-claude-code-project]] | Chat opened with: Can you link up with Claude codes working task |
| [[claude-chat-game-character-creation-tutorial-sources]] | Chat opened with: Hi Claude, can you see if you can find me some sources to put together a tutorial from the follwing pst about creating a game character: https://x.com/insaneUEFN/status/2031406149547811118 |
| [[claude-chat-hi-claude-looks-like-you-got]] | Chat opened with: Hi Claude, looks like you got an update. Can you check on the GND floor automation again and the new sensor |
| [[claude-chat-home-assistant-connection-issue]] | **Conversation Overview** The person reached out to ask whether Claude could access or interact with Home Assistant, a home automation platform. Claude clarified that no Home Assistant connection, file, or integration was visible in the current conversation and asked for more context about what the person was trying to accomplish. The person followed up to ask whether Home Assistant integration with Claude is device-specific, noting they were attempting to access it via iPhone. Claude explained that the Claude mobile app on iPhone does not have a native Home Assistant integration and that connecting Home Assistant to Claude typically requires a more advanced setup involving an MCP (Model Context Protocol) server running locally or remotely, combined with the Claude API or desktop app. Claude noted that while the mobile app offers the same connector options as the web version, custom integrations like Home Assistant are generally harder to configure on mobile due to the local server requirements involved. Claude offered to help the person determine the best approach for their specific use case. |
| [[claude-chat-home-assistant-integration-setup-steps]] | **Conversation overview** This conversation covered two main topics: generating PDF guides for Home Assistant (HA) integrations, and diagnosing a connectivity issue caused by migrating HA to new hardware. The person is running Home Assistant on a Proxmox server (most recently a Lenovo ThinkStation P330 Tiny, previously a Lenovo M70Q Tiny Gen2) and uses a M3 MacBook Pro as their primary workstation. They have an established smart home setup including Zigbee2MQTT, Mosquitto MQTT broker, Cloudflared tunnel, Tailscale, and connect Claude to HA via the Home Assistant MCP Server add-on (slug: `81f33d0f_ha_mcp`). Claude generated two PDF guides: first a general guide for connecting HA to LM Studio on Mac, then an updated Apple Silicon edition tailored to the M3 MacBook Pro. The updated guide added sections on Unified Memory Architecture advantages, MLX model format selection (preferred over GGUF on Apple Silicon, 2-3x faster), a chip/RAM/model-size recommendation table with the M3 Pro 18GB row highlighted, and a full section on LM Link — LM Studio's new Tailscale-powered encrypted cross-device model sharing feature, including setup steps, Claude Code integration, and a multi-device HA architecture diagram. The session then shifted to diagnosing why the "Living Room Lamp - PIR & Presence Control" automation was causing the lamp to turn on every few hours. This was complicated by the HA migration to the new ThinkStation P330 Tiny, which broke the MCP connection. After working through the reconnection (correcting the IP from `.90` to `.56` in the Claude Mac app config at `~/Library/Application Support/Claude/claude_desktop_config.json`), Claude diagnosed the lamp issue: `aqara_high_human_sensor_occupancy` is in a `null`/unavailable state post-migration, and the no_presence off-condition requires all four sensors to be `off` — since `null ≠ off`, the lamp can never automatically turn off, causing the "keeps coming on" pattern. The proposed fix is to remove the Aqara sensor from the no_presence trigger and condition until it is re-paired and working. The person explicitly corrected Claude twice during the connectivity troubleshooting: once for incorrectly suggesting adding `/mcp_server/sse` to the URL path, and once for incorrectly suggesting changing the port to 9583 — both times correctly pointing out that the format had worked before and only the IP address had changed. Claude acknowledged both errors. A key lesson documented was: when migrating HA to new hardware, assign the new machine the same static IP as the old one, since both the Claude MCP config and Cloudflared tunnel reference a hardcoded IP. **Tool knowledge** The Home Assistant MCP Server add-on (slug `81f33d0f_ha_mcp`) connects to the Claude Mac app via `http://[HA-IP]:8123` — no additional path or port suffix required. The MCP Network port shown in the add-on config (9583) is internal and should not be used in the Claude desktop config. The Claude desktop config file lives at `~/Library/Application Support/Claude/claude_desktop_config.json` on macOS and must be updated with a full quit and relaunch of Claude (Cmd+Q, not just closing the window) for changes to take effect. The Cloudflared tunnel config also contains a hardcoded HA IP and must be updated separately when the HA host IP changes. When the MCP server becomes unresponsive, Settings → Add-ons → Home Assistant MCP Server → Restart resolves it without a full HA restart. After a hardware migration, automation traces are wiped; `ha_get_history` with `significant_changes_only: true` over a 48h window is the reliable fallback for diagnosing automation behaviour. The `ha_config_get_automation` tool uses the full entity ID as the identifier parameter (e.g. `automation.living_room_lamp_pir_presence_control`). HTTP-based tools (`ha_get_states`, `ha_config_get_automation`) can succeed while WebSocket-dependent tools (`ha_get_automation_traces`, `ha_get_history`) fail simultaneously, indicating a partial connectivity state where the MCP server HTTP endpoint is reachable but the WebSocket connection has not been established. |
| [[claude-chat-home-assistant-network-architecture-and-smart-device-setup]] | Chat opened with: can you draw me a complete network of how a Homeassistant server would work with recommended smart switches, hubs, extensions etc |
| [[claude-chat-home-assistant-visibility]] | Chat opened with: can you see my home assistant |
| [[claude-chat-installing-a-skill]] | Chat opened with: can you install a skill? |
| [[claude-chat-installing-topview-skill-from-github]] | **Conversation Overview** The person asked Claude to install the Topview AI skill from the GitHub repository `https://github.com/topviewai/skill.git`. Claude cloned the repository and installed it to `/mnt/skills/user/topview/`, which now contains `SKILL.md`, `README.md`, `LICENSE.txt`, `references/`, and `scripts/`. Claude read the `SKILL.md` and summarized the skill's capabilities, which include video generation, image generation and editing, talking avatar creation, background removal, product model shots, and voice/text-to-speech features. Claude then attempted the authentication flow via `python3 scripts/auth.py login`, which failed with a 403 error because `topview.ai` is not within the allowed network domains for this environment. Claude then asked the person to paste their API key directly into the chat — which the person correctly identified as a security risk that should have been proactively flagged. Claude acknowledged this was a mistake and explained safer alternatives: setting `TOPVIEW_API_KEY` and `TOPVIEW_UID` as environment variables, running the auth script locally, or using a secrets manager. The person also asked whether a skill-level guardrail had actually fired to catch the credentials risk, and Claude confirmed it had not — the warning came reactively from Claude's own training, not from any installed skill. The person noted this as a gap. Claude's installed user skills in this environment are: `topview`, `banana-pro-director`, `env-check`, and `token-usage-analyzer`. The `env-check` skill is documented as highest priority and should be read at the start of every session, which Claude had not done. **Tool Knowledge** Bash tool git clone to `/tmp/` followed by `cp -r` to `/mnt/skills/user/[skill-name]/` is the reliable pattern for installing skills in this environment. The `/mnt/skills/user/` directory is writable. Network access to external OAuth endpoints (e.g., `topview.ai`) is blocked in this environment, returning 403 errors; auth flows requiring external domain access must be completed locally and credentials copied in manually or passed via environment variables. |
| [[claude-chat-integrating-claude-with-home-assistant]] | **Conversation Overview** The person is exploring how to integrate Claude AI into their Home Assistant smart home setup, with a focus on using their mobile phone (iPhone 12 Pro Max with the HA Companion app). The conversation covered two main integration approaches: the official Anthropic integration built into Home Assistant (the simpler, recommended starting point) and the more advanced MCP (Model Context Protocol) server add-on approach. Claude walked through the official Anthropic integration setup process, including navigating to Settings → Devices & Services, obtaining an API key from the Anthropic portal, and configuring Claude as a voice assistant accessible via the Assist feature in the iPhone HA Companion app. Claude noted the pay-per-use cost consideration for the API. The person then revealed they are running Home Assistant on a Proxmox server, shifting the conversation toward the MCP setup path. Claude clarified it does not have live visibility into the person's Proxmox environment, but can assist if given details about their configuration. Claude asked clarifying questions about whether HA is running as a VM, LXC container, or Docker on Proxmox, and which MCP deployment approach they prefer. Claude also noted that the MCP approach would allow controlling the home directly through Claude.ai or the Claude app, bypassing the HA Assist interface entirely. The conversation ended with those questions pending, so the MCP setup has not yet been completed. |
| [[claude-chat-living-room-lamp-pir-and-presence-control]] | Chat opened with: can you check the new automation: Living Room Lamp - PIR & Presence Control it did not switch the tv or lamp on when i enabled it. |
| [[claude-chat-mcp-usage-control-and-disable-option]] | **Conversation overview** The person is working on a sophisticated personal AI infrastructure setup on a Mac (hostname Voyager1, username voyager1), combining Claude Desktop, Claude Code, Obsidian (via iCloud), and multiple MCP servers. The conversation covered several interconnected technical problems and setup tasks across MCP management, vault configuration, and knowledge archiving workflows. The person expressed frustration that failing MCP servers (lmstudio and mempalace) were consuming usage quota by erroring on every session. They wanted a simpler enable/disable toggle for MCPs without full removal. Claude explained the current limitation of Claude's interface and recommended removing the broken servers, which the person did by pasting their `claude_desktop_config.json` contents. Claude then created a redacted backup of the full MCP configuration (including homeassistant, mempalace, notebooklm-mcp, and lmstudio entries) saved to the wiki. The person is building a personal LLM Wiki using the Karpathy wiki pattern across two Obsidian vaults: `Obsidian Vault AI` (for AI/LLM research) and `Obsidian Vault Personal` (for personal knowledge). Both vaults live under the iCloud path `BASE="/Users/voyager1/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian Vault Icloud"`. A key correction emerged mid-conversation: earlier sessions had written incorrect placeholder vault paths (`~/Documents/AIAgenticVault`), and the person directed Claude to fix these to the confirmed iCloud paths. Claude Code independently identified and fixed a further path mismatch where the smart-connections MCP's `OBSIDIAN_VAULT_PATH` pointed to the parent `Obsidian Vault Icloud` rather than the correct subdirectory `Obsidian Vault AI` where `.smart-env/multi/*.ajson` embeddings actually live — both `~/.mcp.json` and the Claude Desktop config were corrected. The person confirmed the Smart Connections plugin and `.smart-env` folder are present in `Obsidian Vault AI`. The person noted a preference for using Claude Code for local filesystem tasks (file deletion, path editing, config changes) rather than claude.ai. Three Claude Code slash commands were created and installed: `/aillmwiki` (loads full wiki context, checks `wiki/log.md`, offers ingest/query/lint), `/ingest` (goes directly into 9-step ingest mode without preamble), and the person confirmed "ingest this" is the natural language trigger within the `/aillmwiki` context. A fully automated Python watcher script (`~/scripts/claude_archive_watcher.py`) was also created using the `watchdog` library to monitor `~/Downloads/claude-exports/` for Claude.ai export ZIPs, convert `conversations.jsonl` to dated Markdown files, and save them to `claude-sessions/YYYY/MM/` with a master index at `wiki/claude-sessions-index.md` and launchd auto-start configured. A bonus cron job for nightly Claude Code session syncing was included. **Tool knowledge** Google Drive searches using `'[folder-id]' in parents` are required to navigate into specific subfolders rather than broad name searches, which consistently returned outdated or incorrect files from earlier sessions. The correct folder IDs for this person's vault structure are: `Obsidian Vault Icloud` root = `1lUkNpjLpWcS1bgAj3eGN98JTkEaK6vFV`, `Obsidian Vault AI` subfolder = `1_2kHgJW000jH0RygY4811pbOaTOikaHu`, `wiki` subfolder within AI vault = `1FMofbeqZ70KPKH34HprjfALfpHBOjD9V`. Broad searches like `name contains 'LLM Wiki'` repeatedly surfaced stale Google Drive copies rather than the iCloud-connected files the person was actively working with. Google Drive `create_file` cannot edit existing documents in place — it always creates new files, leaving old versions as duplicates that require manual deletion. The `parentId` parameter must be set explicitly to place files in the correct vault subfolder. The `disableConversionToGoogleType: False` parameter with `contentMimeType: text/plain` was used consistently for markdown files. Conversation search across claude |
| [[claude-chat-mobile-and-desktop-sync-issues]] | Chat opened with: Hi Claude any idea why everything on the mobile is not syncing up from the desktop |
| [[claude-chat-mobile-claude-functionality-test]] | **Conversation Overview** This was a very brief exchange in which the person tested whether Claude was functioning on a mobile platform. The person asked if the mobile Claude experience was working, and Claude confirmed it was operational and asked what the test involved. No further tasks were completed or topics explored beyond this initial connectivity check. |
| [[claude-chat-mobile-test-and-wiki-status-check]] | Chat opened with: Mobile test today |
| [[claude-chat-mqtt-setup-broken-after-hardware-migration]] | **Conversation Overview** The person migrated an NVMe and SSD from a Lenovo M70Q Tiny Gen2 into a Lenovo M70Q Tiny I7 and encountered issues with their Home Assistant MQTT setup afterward. The core problem turned out to be that the Sonoff Zigbee 3.0 USB Dongle Plus V2 was no longer being recognized by Zigbee2MQTT after the hardware swap, as the device path had changed from its previous assignment to `/dev/ttyUSB1`. Claude walked through diagnosing the issue by checking `/dev/serial/by-id/` for the dongle, and the person confirmed it was visible in the HA Hardware list with the full ID path: `/dev/serial/by-id/usb-Itead_Sonoff_Zigbee_3.0_USB_Dongle_Plus_V2_6024afc48fc2ef11a6eac6138148b910-if00-port0`. The recommended fix was to update the Zigbee2MQTT add-on configuration to use this stable by-id path rather than the volatile `ttyUSBx` assignment, preventing recurrence after future reboots. The conversation ended before confirmation that the fix resolved the issue. The person also noted that a direct MCP link Claude had was broken during the session, meaning Claude could not access the HA instance directly and relied on the person providing device information manually. |
| [[claude-chat-pir-first-floor-lights-automation-troubleshooting]] | Chat opened with: i created a new automation for the pir 1st floor lights, but now the lights refuse to come on, can you run a diags? |
| [[claude-chat-pir-lights-first-floor-issue]] | Chat opened with: I found this problem on the PIR lights 1st floor |
| [[claude-chat-productivity-tools]] | **Conversation Overview** The person came to explore building a productivity tool, initially going through a selection process before clarifying their actual goal: an Email Marketing Unsubscriber tool integrated with Gmail. The conversation evolved into deep research territory, covering existing open source solutions, privacy concerns, technical architecture, AI-powered approaches, and real-world limitations of current unsubscribe methods. Through a series of web searches, Claude and the person investigated the landscape of existing tools (Google Apps Script-based, Python, Chrome extensions, and web apps), evaluated their privacy tradeoffs, and identified the RFC 8058 POST-based header method as the safest and most legally enforceable unsubscribe approach. Research confirmed that as of 2024–2025, Gmail, Yahoo, and Microsoft all mandate bulk sender compliance with one-click unsubscribe standards, with Gmail moving to active rejection of non-compliant senders from November 2025 onward. The AI agent fallback approach for complex unsubscribe forms was noted to have approximately an 82% real-world success rate. A key insight emerged from the person's own experience: Mac Mail's native unsubscribe feature is fundamentally unreliable because it sends a mailto request that senders can ignore, provides no confirmation or tracking, and can inadvertently signal to bad actors that an email address is active. This real-world observation strengthened the case for building a custom tool using the RFC 8058 POST method with smart sender scoring, rate limiting, and an AI fallback for complex forms. The person decided to pause and return the following evening to continue research before moving into the build phase. Desired features include one-click and bulk unsubscribe, categorization by sender and type, and progress stats and charts, all with a privacy-first design keeping data within the user's Google account. |
| [[claude-chat-quarterly-projects-summary]] | Chat opened with: Hey Claude, could you summarize my top projects this quarter? Use any connector that would be helpful, and get started once you've gathered enough info. If it makes sense, create something we can… |
| [[claude-chat-reviewing-recent-conversation-highlights]] | **Conversation Overview** This conversation consisted of Claude retrieving and summarizing recent conversation history for the person using the `recent_chats` tool. The summary Claude provided covered four main topic areas from prior sessions. The person had been working through insurance coverage options for camera equipment being loaned to a family member for a short trip abroad. The conversations involved researching specific policies — Ripe Photography Insurance (underwritten by Hiscox), Eversure, and an existing A to Z leisure policy — identifying a key exclusion clause around attendance requirements, and drafting a cover query email to Ripe. Short-term specialist alternatives were also explored. The person has an active GitHub wiki project (`wiki/index.md` and `log.md`) used for tracking findings, with a known limitation that auto-loading only functions correctly within the aiLLMwiki project rather than standard claude.ai chats. This was logged as a documented finding. A NotebookLM-based memory experiment was noted as created but not actively maintained. On the technical integrations side, the person explored ComfyUI via `comfy-cli`, worked on connecting n8n (v2.15.1) to Claude Desktop via the `n8n-mcp` npm package with a local API key, and confirmed that claude.ai cannot reach local IPs directly — with Cloudflare tunnels or Claude Desktop identified as the recommended workarounds. A Spotify playlist ("Dance Remix June 2026") was successfully created via MCP connector. **Tool Knowledge** The `recent_chats` tool was called with parameter `n=10` to retrieve the ten most recent conversations, which successfully returned structured highlights across multiple topic threads. The Spotify MCP connector successfully handled playlist creation without issues. The n8n MCP integration uses the `n8n-mcp` npm package with a locally configured API key rather than direct network connection, which is the reliable approach for this person's setup. |
| [[claude-chat-session-startup-triggers-not-activating]] | Chat opened with: hey chat |
| [[claude-chat-setting-up-ha-mcp-integration-with-home-assistant]] | **Conversation Overview** The person successfully set up the ha-mcp integration (https://github.com/homeassistant-ai/ha-mcp) to connect Claude to their Home Assistant instance running on Proxmox. The session involved troubleshooting a complex chain of configuration issues across multiple systems: Cloudflare Zero Trust tunnels, Home Assistant's HTTP/proxy configuration, and Claude Desktop's MCP connector settings. The person is based in Brighton, UK and has an established Home Assistant setup with 179 entities across 20 domains, including Zigbee devices via ZHA, a Denon AVR receiver, smart lights, a rain sensor, and Music Assistant. The final working architecture uses the Cloudflare Zero Trust tunnel (via the homeassistant-apps/app-cloudflared addon) to expose HA externally at https://ha.pcgamesplay1.com, while Claude Desktop connects directly to the local HA instance at http://[REDACTED:private_ip]:8123 using a long-lived access token. The ha-mcp addon runs on port 9583 with a private path token for authentication. Key configuration fixes included: correcting Cloudflare tunnel routes from HTTPS to HTTP for internal services, fixing YAML indentation errors in configuration.yaml, adding the http: trusted_proxies block at root level (not nested under homeassistant:), and adding subnet [REDACTED:private_ip]/24 to trusted proxies alongside [REDACTED:private_ip]/24. The person plans to work on fixing their automations in a future session. They noted they wanted to save this conversation to a Claude project going forward. **Tool Knowledge** The Home Assistant MCP tools were unreachable throughout most of the session due to the configuration issues being resolved, returning 502 errors and WebSocket failures. Once the local IP was used in Claude Desktop's config (http://[REDACTED:private_ip]:8123) rather than the Cloudflare URL, the WebSocket connection succeeded and full entity access was restored. The ha_get_overview tool with detail_level "full" successfully returned all 179 entities once connected. Cloudflare tunnels strip or fail WebSocket upgrades when routing through the public tunnel URL, making direct local IP connection essential for Claude Desktop MCP when on the same network. Internal Cloudflare tunnel service URLs must use HTTP not HTTPS (e.g., [REDACTED:private_ip]:8123 not https://[REDACTED:private_ip]:8123), as Cloudflare handles TLS termination externally. |
| [[claude-chat-setup-walkthrough]] | **Conversation Overview** This was a long, highly technical session focused on designing and implementing a dual-vault knowledge management system using the Karpathy LLM Wiki pattern. The user (who appears to go by the username "voyager1" based on system paths) is building two separate Obsidian vaults — one for AI and agentic tooling research, one for personal use — using Claude.ai Projects as the interface layer. The session began with evaluating an existing memory system (built around Pinecone for vector storage) and identifying it as heavily Pinecone-specific rather than a generalized framework. Claude rewrote the core skills (wrap-up and recall) for Obsidian and Smart Connections instead. The conversation then pivoted to the Karpathy LLM Wiki pattern after the user recognized it as a better architectural fit, and the remainder of the session was spent implementing it end-to-end. Key decisions made during the session: adopt the Karpathy three-layer pattern (raw sources, wiki layer, schema) for both vaults; use iCloud as the vault storage backend (not Google Drive, which lacks iPhone compatibility and MCP recognition); design a dispatch continuity system using log.md and dispatch-state.md files to solve a previously identified problem where Cowork dispatch workflows could not resume mid-execution due to Claude's memory not updating in real time. The user explicitly identified this memory persistence gap as the likely root cause of failed Cowork dispatch sessions. Multi-agent support was added to both vault designs, with each AI agent (Claude, Cowork dispatch, and any future agents) getting their own folder containing session-log.md, dispatch-state.md, and an inbox. A validated Obsidian plugin starter pack (Smart Connections, Web Clipper, Dataview, Templater, Tasks, Kanban, Kanban Status Updater, Advanced Tables, Omnisearch, QuickAdd, Linter) was documented and saved to the vault, with MCP integration (smart-connections-mcp, obsidian-mcp-plugin) flagged as a later-phase addition. Phase 1 of the AI Agentic vault was completed successfully during the session: the Karpathy LLM Wiki gist was ingested, producing nine wiki pages across concepts/, entities/, and sources/, plus a synthesis page (rag-vs-llm-wiki.md) filed back from a test query. The smart-connections-mcp server (dan6684/smart-connections-mcp) was installed, resolving a Windows line-ending issue with `sed -i '' 's/\r//' install.sh` before running. The MCP server auto-detected the iCloud vault path. A structural correction was identified late in the session: the wiki folders were initially created at the wrong level (iCloud vault root) rather than inside the nested Obsidian Vault AI and Obsidian Vault Personal subfolders. The session ended mid-resolution of this folder structure issue, with the user noting that the MCP server should now give Claude direct filesystem access to the vault. **Tool Knowledge** Google Drive API searches using `'[folder-id]' in parents` syntax work for listing folder contents, but the Drive API cannot read plain text or markdown files — only Google Docs (mimeType `application/vnd.google-apps.document`). This means clippings saved by Obsidian Web Clipper as .md files are invisible to Claude via Drive. The workaround adopted was direct URL fetching via web_fetch for GitHub sources. Folder creation via `Google Drive:create_file` with `mimeType: application/vnd.google-apps.folder` and a `parentId` works reliably; plain text files require `contentMimeType: text/plain` and `disableConversionToGoogleType: True` to avoid Google converting them to Docs format. Drive folder IDs confirmed during session: AI vault root `1kLR9X7tGo4OVOxt0hIC4tpyRHV99oOUI`, wiki subfolder `1rb1uyQNI1R0DDMZUfNgW7tDbK4AOfQsz`, concepts `1WaASv8sBlqusdq3dCLFnZnhJPD8-6wi0`, entities `19DCMAkdWvB4Dh00DlNEiY6F3bhixLywG`, sources `1cVSNqsLAFqfE12Ewmc6 |
| [[claude-chat-skills-in-context]] | Chat opened with: Are any of the skills now in your context? |
| [[claude-chat-sonoff-zigbee-dongle-port-mapping-after-hardware-migration]] | **Conversation Overview** This was an extended technical troubleshooting session focused on recovering a Zigbee2MQTT (Z2M) and ZHA setup in Home Assistant after migrating NVMe and SSD storage from a Lenovo M70Q Tiny Gen2 to an M70Q Tiny I7. The migration caused the Sonoff Zigbee USB Dongle Plus to stop being recognised, breaking the Z2M/MQTT setup. The person runs Home Assistant OS (version 2026.3.4) with Supervisor 2026.03.2 on Proxmox, using two Zigbee dongles: a Sonoff Dongle P (for Z2M, device ID `usb-ITead_Sonoff_Zigbee_3.0_USB_Dongle_Plus_00df5f14b79aef11942fb79061ce3355-if00-port0`) and a Sonoff Dongle E (for ZHA). The Z2M configuration file lives at `/config/zigbee2mqtt/configuration.yaml` and uses Mosquitto broker (`mqtt://core-mosquitto:1883`). The troubleshooting process involved several layers: first correcting the Z2M serial port path in the configuration file via the File Editor add-on, then discovering that both dongles share the same USB vendor/device ID (`10c4:ea60`) in Proxmox, making vendor/device ID-based passthrough ambiguous. The solution was switching both USB passthrough entries in Proxmox from vendor/device ID to port-based assignment. USB protocol errors (`-71 EPROTO`) then emerged from the front USB ports on the new hardware; these were resolved by using a USB 2.0 hub for the front-port dongle. After these fixes, both ZHA and Z2M came back online successfully. The person also clarified an important technical point: devices marketed as "High Human Sensors" (such as the RTCGQ13LM, which is actually an Aqara P1 PIR sensor) are not true always-on MMWave radar sensors — true MMWave sensors require mains power and scan continuously, whereas the PIR-based devices only activate secondary sensing after initial PIR detection. The Aqara P1 PIR sensor (RTCGQ13LM) was paired to Z2M and is planned for bathroom lighting automation testing later in the week. Orphaned ZHA entities from the removed GND floor Aqara device were cleaned up via Settings → Devices & Services → Entities → three-dot menu → Delete (not via checkboxes as Claude initially described incorrectly). The person explicitly does not want entities hidden rather than deleted, as hidden-but-forgotten is considered worse than visible orphans. The MCP server was updated to version 7.2.0 during the session. Several corrections to Claude's guidance were made throughout: the HA navigation path "Settings → Add-ons" is now "Settings → Apps" in version 2026.3.4; the Configuration tab in Z2M Apps only appears when the add-on is running (not stopped); the Z2M UI is only fully visible when the browser window is wider than approximately half the screen width; and entity deletion in HA is done via the device info three-dot dropdown menu, not via bulk checkboxes. The person prefers clean, permanent solutions over workarounds and will not leave orphaned or hidden items unresolved before ending a session. **Tool Knowledge** Claude has MCP access to this person's Home Assistant instance, which proved useful for state checks and entity management but has notable limitations. The MCP API does not support filesystem access, so configuration file edits must be done manually via the File Editor add-on. Entity deletion is not supported via MCP — only hiding/disabling is possible, and the person explicitly rejected this approach. When checking for orphaned entities, `ha_get_states` with explicit entity ID lists returns `ENTITY_NOT_FOUND` for fully deleted entities, which is the reliable way to confirm clean deletion. The `ha_get_device` tool did not reliably surface devices by integration name during this session. For Z2M device state checks, entity IDs follow the pattern `[domain].[z2m_friendly_name_underscored]_[attribute]` — for example `binary_sensor.aqara_high_human_pir_1st_main_bedroom_occupancy`. The Z2M bridge connection state is at `binary_sensor.zigbee2mqtt_ |
| [[claude-chat-stop-motion-border-terrier-prompt-rating]] | Chat opened with: rate this prompt: Full-body shot of a handcrafted Border Terrier stop-motion puppet, wearing a faded blue knitted beanie pulled low over one ear, Wes Anderson's Isle of Dogs aesthetic. Meticulous… |
| [[claude-chat-testing-functionality]] | Chat opened with: does this now work? |
| [[claude-chat-today-s-new-updates]] | Chat opened with: hi there have you got the new updates from today? |
| [[claude-chat-token-usage-analyzer]] | Chat opened with: /token-usage-analyzer |
| [[claude-chat-token-usage-in-home-assistant-diagnosis]] | Chat opened with: can you show me how we used up all of my tokens after just a home assistant diagnosis? |
| [[claude-chat-untitled]] | Empty conversation export (0 messages). |
| [[claude-chat-updating-tutorial-with-project-images]] | Chat opened with: I could add the images into the chat so I had to create a New Project and add all the images into it. Can you now update the Tutorial? |
| [[claude-chat-yaml-frontmatter-requirement-for-skill-md]] | Empty conversation export (0 messages). |
| [[claude-mobile-sync-issues-2026-05]] | Investigation and fixes for Claude.ai mobile sync failures and Mac desktop issues, 2026-05-22. |
| [[claude-skills-agent-discovery-feb2026]] | Feb 2026 exploration of Agent Skills ecosystem: official announcement, SKILL.md format, open standard, Skills vs NotebookLM comparison, mobile gap, Apple/Gemini deal context. |
| [[cli-printing-press]] | Agent-driven Go CLI + MCP server generator — takes any API spec/URL/HAR and produces a ship-ready CLI binary and MCP server with SQLite persistence, FTS5 search, and agent-native output modes. |
| [[cloakbrowser-cloakhq]] | Stealth Chromium fork with source-level fingerprint patches — passes 30/30 bot detection tests, drop-in Playwright replacement. |
| [[codeburn-getagentseal]] | TUI dashboard tracking token cost and performance across 19+ AI coding tools — offline, no proxy, reads local session files. |
| [[dan6684-smart-connections-mcp]] | MCP server that exposes Obsidian Smart Connections vector database to Claude Code via semantic search. |
| [[design-extract-manavarya09]] | CLI + MCP server that extracts a complete design system from any URL — DTCG tokens, Tailwind config, Figma variables, WCAG audit. |
| [[djbrightone-spotify-skill-may2026]] | May 2026 Spotify Monday workflow setup, launchd plist automation, two-app Spotipy architecture, diagnosis of why automation never fired (wrong python path + never installed to LaunchAgents). |
| [[karpathy-guidelines-source]] | Claude Code plugin encoding Andrej Karpathy's 4 behavioral rules for LLM coding: think first, simplicity, surgical changes, goal-driven execution. |
| [[karpathy-llm-wiki-gist]] | Karpathy's April 2026 gist proposing persistent LLM-maintained wikis as an alternative to RAG — the founding pattern for this vault. |
| [[mcp-connector-management]] | 36-message session establishing MCP enable/disable patterns — per-conversation toggles on claude.ai, SwiftBar plugin for Claude Desktop config management, disabled.json pattern. |
| [[semantic-clip]] | Node.js CLI for local semantic search over Smart Connections embeddings — zero MCP, zero network, cosine similarity over .smart-env/multi/*.ajson. |
| [[vault-setup-session]] | Founding session establishing the AI LLM Wiki vault structure, MCP connections, and wiki folder layout in Obsidian iCloud vault. |

## Syntheses

*12 pages*

| Page | Summary |
|---|---|
| [[docx-incident-case-study]] | Claude damaged a Word document while fixing it, then repeated the failure pattern from 3 months prior. First live application of AI Analysis Skill v1.7. |
| [[facs-va-valence-emotion-guide-2026]] | Synthesised guide from FACS VA Valence Micro and Normal Face Cues NotebookLM — 82 sources, AU mappings + AI pipeline applications |
| [[Master AI Creation Guide May 2026]] | Single pristine source of truth. Phase 1–4 pipeline output. 14 canonical models, master JSON template, full production pipeline, constraints table, Gemini Omni reference architecture. |
| [[master-ai-video-image-guide-2026]] | Synthesised master guide from Theoretical Media NotebookLM — 9 parts, 100 sources |
| [[master-comfyui-2026]] | Complete ComfyUI setup guide — installation per OS, VRAM thresholds, exact CFG values, model file paths, launch flags, LTX-2.3 two-stage pipeline, troubleshooting |
| [[master-veo-professional-2026]] | Comprehensive Veo 3.1 professional prompting guide — model specs, syntax formulas, JSON schemas, continuity workflows, troubleshooting |
| [[master-veo-vertex-ai]] | summary: Placeholder synthesis referenced from the Veo professional/April-2026 prompting master pages. |
| [[master-veo3-prompting-april-2026]] | Veo 3.1 prompting from community discovery angle — sonic landscaping, timestamp syntax, hand-over-hand, Edit-dont-reroll, @-mention API syntax |
| [[master-veo3-synthesis]] | summary: Placeholder synthesis referenced from the Veo professional/April-2026 prompting master pages. |
| [[MASTER_AI_CREATION_GUIDE]] | Single pristine source of truth. Phase 1–4 pipeline output. 14 canonical models, master JSON template, full production pipeline, constraints table, Gemini Omni reference architecture. |
| [[rag-vs-llm-wiki]] | RAG re-derives knowledge from scratch on every query; the LLM Wiki compiles it once and keeps it current. |
| [[skill-selection-guide]] | Decision framework for choosing which Claude Code skill to invoke by task type, domain, and workflow phase. |

## Incidents

*10 pages*

| Page | Summary |
|---|---|
| [[incidents/2026-07-11-wsus-dead-placeholder-blocks-fod-install]] | A leftover WSUS policy pointing at a fake internal server blocked OpenSSH.Server (and other Feature-on-Demand) installs with error 0x80072ee6; same root cause as the Defender crash-loop incident. |
| [[incidents/2026-07-30-repair-plan-session-summary]] | Closed R1-R11 repair session: vault-bisync shebang/TCC fix (later lost, re-applied 2026-09-18), 8 dead launchd agents restored from git, SOUL.md injected via SessionStart hook. |
| [[incidents/2026-07-30-soul-not-loaded-ui-hallucination]] | Agent never loaded SOUL.md despite a global instruction, then broke its core trust rule four times, ending by fabricating that no such rule existed. |
| [[incidents/2026-08-11-ha-config-drift-capability-hallucination]] | During HA troubleshooting an agent made a false capability-absence claim (no REST write path), pushed stale Terminal add-on setup steps and ran verification theatre instead of testing the claim. |
| [[incidents/2026-08-11-ha-drift-review-chain-conclusion]] | Unredacted multi-agent conclusion on the 2026-08-11 HA drift incident: false capability claim, discarded contradicting recall hit, verification theatre; findings and repair state. |
| [[incidents/2026-08-14-protonvpn-sysext-activated-disabled]] | ProtonVPN dead 18 days because both network extensions sat in a stale activated_disabled record in db.plist; reinstalls and the GUI toggle reuse that record. |
| [[incidents/2026-08-24-architect-studio-overseer-fake-completion]] | Architect Studio's local job backend only actually implements local ComfyUI job processing; cloud (Higgsfield) jobs silently fall through to a fake "completed" status with a stale output URL — looks successful, dispatched nothing. Also found: wide-open CORS on a LAN-bound port. |
| [[incidents/2026-09-01-buzz-nsec-identity-unrecoverable]] | The Buzz desktop app's "Create identity" screen suggests no nsec is set up, but the user has used Buzz before — only agent-bot keys were found on disk, not a personal key, and the installed build is stale. |
| [[incidents/2026-09-02-torchaudio-abi-version-pin]] | pip auto-resolving torchaudio against a pinned torch version pulled an ABI-incompatible build; only a real boot caught it, not an import check. |
| [[incidents/2026-09-02-vault-syncthing-icloud-drift]] | Both the global CLAUDE.md and the /aillmwiki skill pointed Win11 sessions at a stale iCloud copy of the vault; the real sync target is a Syncthing folder they never referenced, so zero Win11 sessions were ever ingested. |

## Pinokio / Windows 11

*4 pages*

| Page | Summary |
|---|---|
| [[pinokio/pinokio]] | Pinokio is a one-click AI app launcher that installs and manages local AI tools via browser-based scripts. |
| [[pinokio/Windows 11/claude-code-sync]] | Last updated: 2026-05-31. Full current Mac state. |
| [[pinokio/Windows 11/vertex-setup]] | Vertex AI (Gemini) setup for Win11 — gives the Win11 architect-mcp instance its own cloud generation path (redundant with Mac). As of 2026-06. |
| [[pinokio/Windows 11/windows-11]] | Windows 11 Pro — the host OS running the full local AI stack on this machine. |

---

Generated 2026-09-26 by `generate_wiki_index.py`. 141 AI pages listed above + 23 Home Assistant pages in `wiki-ha/` (indexed in `index.html`, not listed here) = **164 total**.

Regenerate with `python3 generate_wiki_index.py` from the vault root. Hand edits will be overwritten.
